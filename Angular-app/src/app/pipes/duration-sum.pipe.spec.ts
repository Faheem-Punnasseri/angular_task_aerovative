import { DurationSumPipe } from './duration-sum.pipe';

describe('DurationSumPipe', () => {
  it('create an instance', () => {
    const pipe = new DurationSumPipe();
    expect(pipe).toBeTruthy();
  });
});
